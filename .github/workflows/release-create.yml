name: Release Create

on:
  workflow_run:
    workflows: [CI]
    types: [completed]

jobs:
  publish:
    name: Publish create-skillet to npm
    runs-on: ubuntu-latest
    if: >
      github.event.workflow_run.conclusion == 'success' &&
      startsWith(github.event.workflow_run.head_branch, 'create-v')
    concurrency:
      group: release-create-${{ github.event.workflow_run.head_sha }}
      cancel-in-progress: false
    steps:
      - name: Validate semver tag
        run: |
          [[ "$BRANCH" =~ ^create-v[0-9]+\.[0-9]+\.[0-9]+$ ]]
        env:
          BRANCH: ${{ github.event.workflow_run.head_branch }}
      - uses: actions/checkout@v6
        with:
          ref: ${{ github.event.workflow_run.head_sha }}
      - uses: pnpm/action-setup@v6
      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
          registry-url: https://registry.npmjs.org
      - run: pnpm install --frozen-lockfile
        env:
          LEFTHOOK: 0
      - run: pnpm --filter @skillet-cli/ui build
      - run: pnpm -F create-skillet publish --access public --no-git-checks
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
