# Session Log: <repo-name>

```
repo:
tier:
env:
version:
date:
tester:
docker-image:
```

---

**This file is append-only.** Record observations as they happen, prefixed with `HH:MM`. Never edit or delete prior entries — if you need to correct something, add a new entry noting the correction.

---

<!-- Append entries below.

Format examples:

14:32 Test user ran `npx create-skillet` unprompted. Wizard launched. Selected all defaults.
14:35 Output directory present. Test user inspected contents without guidance from session.
14:38 npm pack failed — "missing main field" error. Test user tried parent directory, same result. → ISS-001
14:42 Test user found workaround by adding "main" field manually to package.json. Continued. → ISS-001 (workaround noted)
14:47 Skill files landed at ~/.claude/skills/my-skill/. Matches expected path. Step 6 pass.
14:50 Test user completely stuck on Step 5 — could not determine install path from available docs. Session ended.

-->
