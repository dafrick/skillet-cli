# Session Log: <repo-name>

```
repo:
tier:
env:
date:
tester:
docker-image:
```

---

**This file is append-only.** Entries are NEVER edited or deleted once written. Record observations as they happen, prefixed with the current time in `HH:MM` format. Do not go back and revise earlier entries — if you need to correct something, add a new entry noting the correction.

---

<!-- Append entries below. Example format:

14:32 Ran create-skillet against the repo. Wizard appeared. Selected defaults for all prompts.
14:35 Output directory looked correct. Moving to npm pack step.
14:38 npm pack failed with "missing main field" error. → ISS-001

-->
