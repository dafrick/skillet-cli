import { describe, it } from 'vitest';

describe('registry', () => {
  it.todo('register() adds an adapter');
  it.todo('get(id) returns the registered adapter');
  it.todo('list() returns all registered adapters');
  it.todo('registering a duplicate id throws');
  it.todo('registerAdapter is an alias for registry.register');
});
