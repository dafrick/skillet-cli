import { describe, it } from 'vitest';

describe('ui/wordmark', () => {
  it.todo(
    'returns a non-empty multi-line string with ANSI codes when process.stdout.isTTY is true',
  );
  it.todo('returns an empty string when process.stdout.isTTY is false');
});
