import { describe, it } from 'vitest';

describe('ui/verbs', () => {
  it.todo('pickVerb("install") returns a pair from the install verb pool');
  it.todo('verbs are sentence-case when isTTY is true');
  it.todo('verbs are lowercase when isTTY is false');
});
