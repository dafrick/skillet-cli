import { describe, it } from 'vitest';

describe('ui/colors', () => {
  it.todo('color token functions produce ANSI escape sequences when isTTY is true');
  it.todo('color token functions produce plain strings when isTTY is false');
});
