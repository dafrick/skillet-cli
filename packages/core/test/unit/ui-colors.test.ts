import { describe, it } from 'vitest';

describe('ui/colors', () => {
  it.todo('color token functions produce ANSI escape sequences when NO_COLOR is unset');
  it.todo('color token functions produce plain strings when NO_COLOR=1 is set');
  it.todo('structure (non-color output) is preserved when NO_COLOR=1 is set');
});
